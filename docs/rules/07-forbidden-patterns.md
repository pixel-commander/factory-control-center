# Forbidden patterns

- Flexbox
- inline style={}
- ad hoc custom classes created in shared or global style files
- creating new shared utilities just to avoid a local class
- custom grid templates outside HOUSE_CSS grid.css
- invented prop keys
- invented names for standard house props
- custom tokens without approval
- object spread onto DOM nodes
- generic stamping that ignores the house system
