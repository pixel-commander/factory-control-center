# Demos and registry rules

- Demo files should export const Demo and default Demo.
- Keep the demo folder pattern consistent with the house system.
- Use the manifest system instead of hand-writing demo pages when possible.
- Keep the demo names and folder names aligned with the component registry.
- Use local CSS and local state for demo-only behavior.
