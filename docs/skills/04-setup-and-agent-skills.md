# Setup and agent skills

Use these when setting up the environment or agent context.

- Keep the environment config minimal and explicit.
- Prefer existing repo tools and package scripts over new infrastructure.
- Use the available repo scripts before inventing new commands.
- When setting up a new task, capture the exact goal and constraints.
- Keep the working rules explicit for future agents.
